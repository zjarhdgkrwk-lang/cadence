# SSOT — 로컬 음악 플레이어 (Windows 전용)

> 단일 진실 공급원(Single Source of Truth). 이 문서는 프로젝트의 요구사항·범위·기술 결정·데이터 모델·화면 구조를 한곳에 정의한다. Claude Code(개발)와 Claude Design(UI)이 이 문서를 기준으로 작업한다. 변경 시 반드시 이 문서를 먼저 갱신한다.

- 문서 버전: v1.1 (메이저 음악 앱 수준의 디자인·UX 기준 추가)
- 대상 플랫폼: Windows 전용 (Windows 10 / 11)
- 작업 도구: 개발 = Claude Code(웹), 디자인 = Claude Design
- 워킹 타이틀: 미정 (변경 가능)

---

## 1. 제품 개요

인터넷 연결 없이 동작하는 개인용 로컬 음악 재생 플레이어. 사용자가 지정한 폴더의 음악 파일과 `.lrc` 가사 파일을 스캔해 라이브러리를 구성하고, 일반적인 음악 플레이어의 기능 전반과 더불어 가사 동기화 표시, 사용자 태그, 직관적인 드래그 앤 드롭 큐/플레이리스트 관리를 제공한다.

설계 원칙:

- 완전 오프라인. 어떤 기능도 네트워크를 요구하지 않는다.
- 원본 파일 보호. 메타데이터 편집은 앱 DB에만 저장하고 원본 태그는 건드리지 않는다.
- 직관적인 조작. 드래그 시 항목이 떠다니는(플로팅) 피드백, 실시간 검색, 명확한 큐/플레이리스트 구분.
- 메이저 음악 앱 수준의 완성도. 시각 디자인·모션·인터랙션·성능 모두 상용 메이저 앱(Spotify·Apple Music 등)의 체감 품질을 목표로 한다. 단, 메이저 앱의 온라인 요소(추천/소셜/온라인 가사 검색)는 비범위이며, 여기서 말하는 "수준"은 디자인과 UX의 완성도를 뜻한다. 구체 기준은 §11.

---

## 2. 기술 스택

### 2.1 데스크톱 셸
- **Tauri 2.x** (Rust 백엔드 + WebView2 프런트엔드). Electron 대비 가벼운 번들·메모리, 무거운 작업(스캔·메타데이터·DB)을 Rust에서 고속 처리. Windows에서는 WebView2(Chromium 기반)를 사용하므로 대상 코덱 지원이 양호하다.

### 2.2 프런트엔드
- **React 19 + TypeScript + Vite**
- **Tailwind CSS + shadcn/ui** — Claude Design 산출물을 컴포넌트화하기 적합
- **Zustand** — 전역 상태(플레이어/큐/라이브러리/UI)
- **@dnd-kit** — 드래그 앤 드롭. `DragOverlay`로 들고 있는 항목이 떠다니는 UX 기본 제공, 다중 선택 드래그 지원
- **Motion (framer-motion)** — 페이지 전환, 재생 화면 확장/축소, 호버 노출, 드래그 등 의도된 모션. `prefers-reduced-motion` 존중
- **@tanstack/react-virtual** — 대용량 라이브러리(수만 곡) 리스트 가상화로 60fps 유지
- **lucide-react** — 아이콘

### 2.3 오디오 엔진 (중요 결정)
- **기본(v1): 프런트엔드 HTML5 `<audio>` 엔진.** 두 개의 오디오 엔진 인스턴스(현재/다음)를 두고 다음 곡을 미리 디코딩·프리로딩하여 **근접 갭리스(near-gapless)** 재생을 구현한다.
  - 장점: 재생 속도 조절 시 피치 보존(`preservesPitch`), 정확한 탐색, 폭넓은 코덱 지원, 가사 동기화에 필요한 빈번한 위치 업데이트(`timeupdate`)를 저비용으로 확보.
  - 한계: 연속 녹음 앨범의 트랙 이음새에서 미세한 틈이 남을 수 있음(완전한 샘플 단위 갭리스는 비보장).
- **업그레이드 경로(필요 시): Rust 측 자체 엔진** (`symphonia` 디코딩 + `rodio`/`cpal` 출력, Sink 큐로 진짜 갭리스). v1에서는 채택하지 않음. 향후 갭리스 품질이 불충분하다고 판단될 때만 도입을 검토한다.

### 2.4 Rust 백엔드(Tauri 커맨드/플러그인)
- 폴더 스캔: `walkdir`
- 메타데이터·앨범아트 추출: **`lofty`** (ID3v2, Vorbis comments, MP4, FLAC 등 + 임베디드 커버)
- 앨범아트 주요 색상 추출(적응형 색상용): 스캔 시 `image` + `kmeans-colors`(또는 평균/대표색 추출)로 대표 색을 계산해 DB에 저장. 전부 로컬 계산(오프라인)
- 폴더 변경 감지(자동 재스캔): `notify`
- 데이터 저장: **SQLite** (`tauri-plugin-sql`), 검색 가속용 **FTS5** 가상 테이블
- 미디어 키 + SMTC(Windows 미디어 오버레이): **`souvlaki`** (재생/일시정지/이전/다음 하드웨어 키 및 시스템 미디어 컨트롤 연동)
- 창 상태 복원: `tauri-plugin-window-state`
- 트레이: Tauri 트레이 API

### 2.5 LRC 파싱
- 외부 라이브러리 없이 TypeScript 소형 파서로 `[mm:ss.xx]` 타임스탬프를 파싱. 곡별 오프셋(±)을 적용해 표시.

> 각 의존성의 정확한 버전은 스캐폴딩 시점에 최신 안정 버전으로 설치한다(여기서 버전을 못박지 않음).

---

## 3. 기능 요구사항

### 3.1 라이브러리 스캔
- 여러 폴더(루트) 등록 가능, 하위 폴더 **재귀** 스캔.
- 지원 포맷: `mp3`, `flac`, `wav`, `m4a/aac`, `ogg`, `opus`.
- 폴더 변경 시 **자동 재스캔**(`notify` 감시). 수동 새로고침도 제공.
- 파일이 사라지면 라이브러리에 "없음" 상태로 표시한 뒤 다음 스캔에서 제거.
- 앨범아트 우선순위: 임베디드 커버 → 없으면 폴더 내 이미지(`cover.jpg`, `folder.jpg` 등). 추출 이미지는 캐시 디렉터리에 저장.

### 3.2 재생 컨트롤 (재생 중 바)
- 재생/일시정지, 다음 곡, 이전 곡.
- 랜덤 재생(셔플) 토글 — 반복 모드와 **독립적**.
- 반복 모드 4종(단일 enum):
  1. 한 곡만 재생 (현재 곡 끝나면 정지)
  2. 한 곡 반복 (현재 곡 무한 반복)
  3. 큐 전체 반복 (큐를 끝까지 재생 후 처음으로)
  4. 반복 없이 전체 재생 (큐를 끝까지 재생 후 정지)
- A–B 반복: A·B 지점 지정 후 구간 무한 반복. 활성 동안 반복 모드보다 우선. 진행 바에서 A/B 마커 표시.
- 진행 바(탐색), 볼륨, 음소거.
- 재생 속도 조절: **0.1배 단위**(예: 0.5×~2.0×), 피치 보존.
- 갭리스 재생: 다음 곡 프리로딩 기반 근접 갭리스(§2.3).
- 종료 시 마지막 곡 / 재생 위치 / 큐 / 반복·셔플·볼륨·속도 상태 복원.

### 3.3 가사 (LRC)
- 매칭 규칙: 동일 파일명의 `.lrc` 우선 → 없으면 임베디드 가사 fallback.
- 현재 가사 줄 하이라이트(위치 동기화 스크롤).
- 가사 없을 때 "가사 없음" 표시.
- 곡별 싱크 오프셋(±) 미세조정, 곡별로 DB에 저장.
- 앱 내 LRC 타임스탬프 라인 편집: **v1 범위 외**(향후 선택 기능). 오프셋 조정으로 대체.

### 3.4 큐 / 플레이리스트
- 앨범·플레이리스트·검색결과 등을 재생하면 현재 큐를 **교체**.
- 우클릭 컨텍스트 메뉴: "다음에 재생", "큐에 추가" 제공.
- 현재 재생 큐 패널: 곡 목록 확인 + 드래그로 순서 변경.
- 플레이리스트 생성/이름변경/삭제. 한 곡이 여러 플레이리스트에 중복 포함 허용.
- 플레이리스트 내 곡 순서 드래그 변경.
- m3u **가져오기 / 내보내기** 둘 다 지원.
- 스마트 플레이리스트(태그 자동 채움): 범위 외.

### 3.5 사용자 태그
- 메타데이터 장르와 별개의 **자유 입력** 사용자 태그.
- 태그 색상 지정.
- 다중 선택 후 일괄 태깅.
- 태그 기반 검색/필터: **AND / OR 조합** 모두 지원.

### 3.6 검색
- 통합 검색(제목/아티스트/앨범/태그 전체) + 필드별 필터 토글.
- 타이핑 즉시 반영되는 실시간 검색(FTS5 활용).
- 검색 결과에서 곡 재생 시 **검색 결과 집합으로 큐를 구성**.

### 3.7 드래그 앤 드롭
- 큐 순서 변경.
- 플레이리스트 내 순서 변경.
- 라이브러리/리스트에서 플레이리스트로 끌어 추가.
- 다중 선택 후 한꺼번에 드래그.
- 드래그 중 항목은 `DragOverlay`로 떠다니는 형태로 표시.

### 3.8 파일 작업 (원본 파일 대상)
- 앱에서 파일 **이동 / 이름변경 / 삭제** 가능.
- 이동·이름변경 시 DB의 경로를 갱신.
- **삭제는 한 번 더 확인**(확인 다이얼로그) 후 실행.

### 3.9 시스템 통합 (Windows)
- 미디어 키(키보드 재생/정지/이전/다음) 연동.
- SMTC(Windows 시스템 미디어 오버레이) 연동: 곡 정보·앨범아트·컨트롤 표시.
- **창 닫기(X) = 앱 종료** (재생 정지 포함). 트레이 상주는 v1 비범위 — 사용성 문제(작업 표시줄 미표시, 강제 종료 필요)로 채택하지 않음.
- 기본 단축키: `Space` = 재생/일시정지 (그 외 일부 기본 단축키, §7.4).

### 3.10 UI / 테마
- 레이아웃: 좌측 사이드바(라이브러리 / 플레이리스트 / 태그) + 메인 리스트 + 하단 재생 중 바.
- 라이트 / 다크 테마, **라이트 기본**.
- 미니 플레이어 모드 = **같은 창의 컴팩트 레이아웃**으로 전환(별도 창 아님 — 창 간 오디오/상태 공유 복잡성 회피). 작은 크기에서 핵심 컨트롤만 표시.

### 3.11 전체 화면 재생(Now Playing) 뷰
- 하단 바에서 확장하면 큰 앨범아트 + 동기화 가사 + 큐를 함께 보는 전체 화면 재생 뷰로 전환.
- 현재 곡 앨범아트의 주요 색상을 배경/강조색에 반영(적응형 색상). 곡 전환 시 색상 부드럽게 전환.
- 확장/축소는 모션으로 자연스럽게 연결.

### 3.12 라이브러리 / 상세 뷰
- 앨범 뷰 / 아티스트 뷰: 아트 헤더가 있는 상세 화면. 앨범 클릭 → 트랙 목록 + 한 번에 재생/큐 추가.
- 내장 뷰: 최근 추가, 최근 재생, 많이 들은 곡(이미 저장하는 `date_added`·`last_played_at`·`play_count` 기반의 정렬/필터 뷰). 사용자가 규칙을 정의하는 '스마트 플레이리스트'와는 다른, 고정 내장 뷰임.

---

## 4. 메타데이터 쓰기 정책

- 사용자가 제목/아티스트/앨범/태그 등을 편집해도 **원본 파일 태그는 절대 수정하지 않는다.** 모든 편집은 앱 DB에만 저장.
- 표시 우선순위: DB의 사용자 편집 값 → 없으면 파일에서 읽은 원본 메타데이터.
- 파일 작업(이동/이름변경/삭제)은 §3.8 정책을 따른다(삭제는 이중 확인).

---

## 5. 비범위 (하면 안 되는 것)

- 인터넷 연결을 요구하는 모든 기능(온라인 가사/메타데이터 검색, 스트리밍, 업데이트 체크 등).
- macOS / Linux 빌드 및 지원. Windows 전용.
- EQ / 비주얼라이저.
- 스마트 플레이리스트.
- (v1) 앱 내 LRC 타임스탬프 편집.
- 원본 파일 태그 직접 수정.
- **(v1) 시스템 트레이 상주** (X 버튼 = 앱 종료가 더 예측 가능. §3.9 참조).

---

## 6. 데이터 모델 (SQLite)

> 컬럼은 구현 단계에서 가감 가능. 핵심 구조를 정의한다.

### 6.1 테이블

| 테이블 | 핵심 컬럼 | 설명 |
|---|---|---|
| `folders` | id, path, recursive, added_at | 감시 대상 루트 폴더 |
| `tracks` | id, path(unique), title, artist, album, album_artist, genre, track_no, disc_no, year, duration_ms, sample_rate, bitrate, codec, has_embedded_art, art_cache_path, dominant_color, lrc_path, lyrics_source, lrc_offset_ms, missing(bool), date_added, last_played_at, play_count | 원본 메타 + 사용자 편집 + 재생 통계 + 적응형 색상 |
| `playlists` | id, name, created_at, updated_at | 플레이리스트 |
| `playlist_items` | playlist_id, track_id, position | 플레이리스트-곡 (순서) |
| `tags` | id, name(unique), color | 사용자 태그 |
| `track_tags` | track_id, tag_id | 곡-태그 다대다 |
| `queue_items` | position, track_id | 현재 재생 큐(순서) |
| `app_state` | key, value | current_track_id, current_position_ms, current_queue_index, repeat_mode, shuffle, volume, speed, theme, window/미니플레이어 상태 등 키-값 |
| `tracks_fts` | (FTS5) title, artist, album | 실시간 검색 가속용 가상 테이블 |

### 6.2 비고
- `lyrics_source`: `lrc_file` | `embedded` | `none`.
- `missing=true`인 트랙은 "없음" 표시, 다음 스캔에서 제거.
- 사용자 편집 값과 원본 값을 구분 저장할지 여부는 구현 시 결정(별도 `*_override` 컬럼 또는 단일 컬럼 + 원본 캐시). 표시 우선순위는 §4.

---

## 7. 화면 / UX 명세

### 7.1 레이아웃
- 좌측 사이드바: 라이브러리(전체 곡/앨범/아티스트), 플레이리스트 목록, 태그 목록 및 태그 필터(AND/OR 토글).
- 메인 영역: 현재 컨텍스트(라이브러리/플레이리스트/검색결과)의 트랙 리스트. 컬럼 정렬, 우클릭 컨텍스트 메뉴(재생, 다음에 재생, 큐에 추가, 플레이리스트에 추가, 태그, 파일 작업).
- 우측 패널(토글): 현재 재생 큐 / 가사 뷰 전환.
- 하단 재생 중 바: 앨범아트·곡정보, 재생 컨트롤(§3.2), 진행 바(A/B 마커), 볼륨, 속도, 셔플/반복 토글, 큐·가사 패널 토글.

### 7.2 가사 뷰
- 현재 줄 하이라이트 + 자동 스크롤. 오프셋 조정 컨트롤(±). 가사 없을 때 "가사 없음".

### 7.3 드래그 앤 드롭 UX
- `@dnd-kit` + `DragOverlay`로 드래그 항목 플로팅 표시. 드롭 위치 인디케이터. 다중 선택(Ctrl/Shift) 후 묶음 드래그.

### 7.4 기본 단축키 (초안)
| 키 | 동작 |
|---|---|
| `Space` | 재생 / 일시정지 |
| `Ctrl + →` / `Ctrl + ←` | 다음 / 이전 곡 |
| `→` / `←` | 짧은 구간 탐색 |
| `↑` / `↓` | 볼륨 |
| `Ctrl + F` | 검색 포커스 |
| `S` | 셔플 토글 |
| `R` | 반복 모드 순환 |

---

## 8. 프로젝트 구조 (제안)

```
music-player/
├─ src-tauri/                  # Rust 백엔드
│  ├─ src/
│  │  ├─ main.rs
│  │  ├─ lib.rs
│  │  ├─ commands/             # scan, metadata, file_ops, lrc, playlist, tag, query
│  │  ├─ db/                   # 연결, 마이그레이션, 모델
│  │  ├─ scan/                 # walkdir + lofty + 아트 추출
│  │  ├─ watcher/              # notify (자동 재스캔)
│  │  ├─ media_controls/       # souvlaki (SMTC + 미디어 키)
│  │  └─ state.rs
│  ├─ migrations/
│  ├─ Cargo.toml
│  └─ tauri.conf.json
├─ src/                        # React 프런트엔드
│  ├─ main.tsx
│  ├─ App.tsx
│  ├─ components/
│  │  ├─ layout/               # Sidebar, MainView, NowPlayingBar, MiniPlayer
│  │  ├─ player/               # Controls, ProgressBar, Volume, Speed, ABRepeat
│  │  ├─ lyrics/               # LyricsView
│  │  ├─ queue/                # QueuePanel
│  │  ├─ library/              # TrackList, TrackRow
│  │  ├─ playlists/
│  │  ├─ tags/                 # TagManager, TagFilter
│  │  ├─ search/
│  │  ├─ dnd/                  # Sortable 래퍼, DragOverlay
│  │  └─ ui/                   # shadcn 컴포넌트
│  ├─ stores/                  # zustand: player, queue, library, ui
│  ├─ hooks/                   # useAudioEngine, useLyrics, useMediaKeys, useShortcuts
│  ├─ lib/                     # lrcParser, ipc 래퍼, types
│  └─ styles/
├─ package.json
└─ README.md
```

- 오디오 핵심: `useAudioEngine` 훅이 현재/다음 `<audio>` 인스턴스, 프리로딩, 갭리스 핸드오프, A–B 반복, 속도, 위치 이벤트(가사 동기화)를 관리.
- Rust ↔ JS: 스캔 진행률·파일 변경·미디어 키 이벤트는 Tauri 이벤트로 프런트에 전달.

---

## 9. 개발 단계 (제안)

1. 스캐폴딩 + 디자인 시스템: Tauri 2 + React 19 + TS + Tailwind + shadcn/ui + Zustand + SQLite 마이그레이션. 디자인 토큰(§11.1)과 라이트/다크 테마 기반 먼저 확립.
2. 스캔·메타데이터·DB: 폴더 등록 → walkdir/lofty 스캔 → tracks 적재 → 아트 캐시 + 주요 색상 추출 → 가상화 리스트로 라이브러리 표시.
3. 재생 코어: HTML5 오디오 엔진, 재생 컨트롤, 진행/볼륨, 반복 4모드 + 셔플, 큐, 상태 복원.
4. 갭리스(프리로딩), 속도 조절, A–B 반복.
5. 가사: LRC 파서, 하이라이트/스크롤, 오프셋, 임베디드 fallback.
6. 플레이리스트 + 큐 패널 + @dnd-kit DnD(다중 선택, 플로팅).
7. 태그(색상, 일괄, AND/OR 필터) + 검색(FTS5, 실시간, 검색결과 큐화).
8. 시스템 통합: souvlaki(SMTC/미디어 키), 트레이, 단축키, 창 상태 복원.
9. 파일 작업(이동/이름변경/삭제 이중확인), m3u 가져오기/내보내기.
10. 전체 화면 재생 뷰(적응형 색상), 앨범/아티스트 상세 뷰, 내장 뷰.
11. 완성도 패스: 모션, 호버/툴팁/토스트/스켈레톤/빈 상태, 미니 플레이어, 접근성·성능 점검(§11).

---

## 10. 미해결 / 추후 결정

- 워킹 타이틀(앱 이름) 확정.
- 단축키 세트 최종안.
- 트레이: "닫기 → 트레이" vs "최소화 → 트레이" 동작 선택.
- 사용자 편집 메타데이터 저장 방식(override 컬럼 vs 단일 + 원본 캐시).
- 갭리스 품질 검증 후 Rust 엔진 도입 여부.

---

## 11. 디자인 & UX 표준 (메이저 앱 수준 목표)

> Claude Design과 Claude Code가 일관된 완성도를 향하도록 하는 기준. "예쁘게"가 아니라 측정 가능한 규칙으로 정의한다.

### 11.1 디자인 토큰 (시스템)
- 간격 스케일(예: 4·8·12·16·24·32), 타이포 스케일(명확한 위계: 제목/소제목/본문/캡션), 모서리 반경 스케일, 그림자/엘리베이션 스케일을 토큰으로 정의하고 전 컴포넌트가 이를 따른다.
- 색상은 의미 기반 토큰(배경/표면/경계/전경/강조/위험 등)으로 정의하고 라이트·다크 각각 별도 값으로 튜닝(단순 반전 금지).
- 모든 토큰은 CSS 변수 + Tailwind 테마로 노출.

### 11.2 적응형 색상
- 현재 곡 앨범아트의 주요 색상(`dominant_color`)을 전체 화면 재생 뷰의 배경/강조에 반영. 곡 전환 시 부드러운 색상 트랜지션.
- 대비(접근성)를 확보하기 위해 추출 색 위에 적절한 명도 보정 레이어 적용.

### 11.3 모션
- 의도된 곳에만 사용: 페이지/뷰 전환, 전체 화면 재생 확장·축소, 호버 시 액션 노출, 드래그 플로팅, 토스트 등장/퇴장, 가사 줄 전환.
- 표준 지속시간(짧은 상호작용 ~150–250ms)과 일관된 이징. 과한 바운스/지연 금지.
- `prefers-reduced-motion`을 존중해 모션 축소 모드 제공.

### 11.4 인터랙션 디테일
- 트랙 행: 호버 시 재생/더보기 액션 노출, 현재 재생 곡 강조.
- 진행 바: 호버 시 해당 지점 시간 툴팁, 드래그 시킹. A/B 마커 시각화.
- 볼륨: 스크롤로 조절.
- 모든 사용자 동작에 명확한 피드백: 토스트("큐에 추가됨", "플레이리스트에 추가됨"), 낙관적 업데이트.
- 컨텍스트 메뉴(우클릭)를 전 영역에서 일관되게 제공.

### 11.5 상태 표현
- 로딩: 스켈레톤(빈 화면/스피너 남발 금지). 스캔 진행률은 명확한 인디케이터로.
- 빈 상태: 폴더 미등록·검색 결과 없음·빈 플레이리스트 등에 안내 문구 + 다음 행동 유도.
- 오류: 파일 없음/재생 실패 등은 조용히 무시하지 말고 비파괴적으로 안내.

### 11.6 성능 (체감 품질의 핵심)
- 대용량 라이브러리 리스트는 가상화(`@tanstack/react-virtual`)로 수만 곡에서도 부드럽게.
- 앨범아트는 캐시된 적정 크기 썸네일 사용(원본 디코딩 남발 금지).
- 무거운 작업(스캔·메타데이터·색상 추출)은 Rust에서 처리하고 진행률만 이벤트로 전달.
- 목표: 주요 상호작용 60fps, 입력 즉시 반응.

### 11.7 접근성 & 일관성
- 키보드만으로 핵심 조작 가능(§7.4 단축키 + 포커스 링).
- 충분한 색 대비, 적절한 폰트 크기.
- 아이콘·간격·용어를 전 화면에서 일관되게 유지.
